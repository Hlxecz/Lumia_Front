# 🚨 [중요] 실행 전 꼭 확인해 주세요! (교수님 필독)

> **교수님! 혹시 앱을 실행한 뒤 로그인이나 회원가입 과정에서 무한 로딩이 발생하거나 통신 오류가 발생한다면, 현재 백엔드 서버가 무료 배포 계정(Render/Aiven)으로 작동하고 있어서 그렇습니다.**
>
> 무료 서버 특성상 일정 시간 동안 사용하지 않으면 자동으로 **'잠듬(Sleep) 모드'**로 전환되며, 새로운 요청을 받았을 때 잠에서 깨어나 정상 작동하기까지 약 **1분 ~ 3분 이상** 소요될 수 있습니다.
>
> 혹시라도 정상적인 진행이 안 되신다면, 아래 연락처로 문자나 연락 한 번 주시면 제가 즉시 서버를 깨워 활성화해 드리겠습니다!
>
> 📞 **긴급 활성화 연락처: 010-4063-2661**

---

## 💻 구동을 위한 요구 스펙 및 개발 환경

프론트엔드 프로젝트를 실행하기 위해 필요한 사양과 소프트웨어 목록입니다.

### 1. 개발 장비 (PC) 요구 사양
* **OS**: Windows 10/11, macOS, 또는 Linux
* **Node.js**: **v18 이상** (LTS 버전 권장, Node v20 또는 v22)
* **패키지 관리자**: Node.js 설치 시 포함된 `npm` 사용

### 2. 모바일 기기 요구 사양
* **Android**: Google Play Store에서 **Expo Go** 앱 설치 완료
* **iOS (iPhone)**: App Store에서 **Expo Go** 앱 설치 완료
* 🚨 **네트워크 필수 조건**: 실행을 담당하는 **PC**와 모바일 앱 테스트를 진행할 **휴대폰**이 **반드시 동일한 Wi-Fi 공유기(네트워크)**에 연결되어 있어야 정상 작동합니다.

---

## 🛠️ 프로젝트 설치 및 실행 방법

아래 순서대로 PC의 터미널(PowerShell, CMD, Git Bash 등)에서 명령어를 차례대로 입력해 주세요.

### Step 1. 프로젝트 패키지 설치
깃허브에서 클론 받거나 다운로드한 프로젝트 폴더 내에서 터미널을 열고 다음 명령어를 실행하여 의존성 라이브러리를 설치합니다.
```bash
npm install
```

### Step 2. 개발 서버(Metro) 시작
설치가 완료되면 아래 명령어 중 하나를 입력해 Expo 개발 서버를 실행합니다.
```bash
npm run start
```
*(또는 `npx expo start`를 직접 터미널에 입력하셔도 됩니다.)*

### Step 3. 모바일 기기(Expo Go)와 연동하여 앱 실행

개발 서버가 정상적으로 시작되면 터미널 화면에 커다란 **QR 코드**가 출력됩니다.

#### 📱 Android 폰인 경우:
1. 스마트폰에서 **Expo Go** 앱을 실행합니다.
2. 메인 화면에 있는 **"Scan QR code"**를 선택합니다.
3. PC 터미널 창에 표시된 QR 코드를 비추어 스캔하면 번들링 로딩 후 앱이 실행됩니다.

#### 🍏 iOS (iPhone)인 경우:
1. 스마트폰 기본 **카메라 앱**을 실행합니다.
2. PC 터미널 창에 표시된 QR 코드를 비추어 링크 팝업을 띄웁니다.
3. 팝업을 누르면 **Expo Go** 앱이 자동으로 열리며 앱이 실행됩니다.

---

## 💡 네트워크 연결이 안 될 때 (무한 대기 또는 에러 발생 시)

PC와 휴대폰이 같은 와이파이에 연결되어 있음에도 보안 정책(학교 방화벽 등)이나 공유기 설정으로 인해 기기 간 직접 연결이 불가능할 경우, 아래 명령어를 통해 **터널(Tunnel) 모드**로 개발 서버를 우회 구동할 수 있습니다.

```bash
npx expo start --tunnel
```
*(터널 모드로 켜면 동일한 공유기 망에 있지 않아도 다른 Wi-Fi나 LTE 망에서도 정상적으로 스캔하여 접속할 수 있습니다.)*
