# Frontend example config
# Copy this file to `.env` for local development.

# Backend base URL for the Expo app.
EXPO_PUBLIC_API_BASE_URL=https://lumia-app-server.onrender.com

# Never store OpenAI keys in the frontend app.
# Keep OPENAI_API_KEY on the backend only and call your backend from the app.
